<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mbc.mid.dao.PaymentDao">

	<!-- 결제 정보 저장 -->
    <insert id="insertPayment" parameterType="com.mbc.mid.dto.PaymentDto">
        INSERT INTO payment (parking_log_id, mem_id, amount, pay_method, pay_date)
        VALUES (#{parkingLogId}, #{memId}, #{amount}, #{payMethod}, now())
    </insert>

    <!-- parking_log_id로 결제 정보 조회 (사전정산 pay_date 확인용) -->
    <select id="findByParkingLogId" parameterType="long" resultType="com.mbc.mid.dto.PaymentDto">
        SELECT 
            pay_id          AS payId,
            parking_log_id  AS parkingLogId,
            mem_id          AS memId,
            amount,
            pay_method      AS payMethod,
            pay_date        AS payDate
        FROM payment
        WHERE parking_log_id = #{parkingLogId}
        ORDER BY pay_date DESC
        LIMIT 1
    </select>

    <!-- 진료 완료 여부 확인 (당일 진료 완료) -->
    <select id="checkClinicVisit" parameterType="string" resultType="int">
        SELECT COUNT(*)
        FROM reservation r
        JOIN member_vehicle v ON r.mem_id = v.mem_id
        WHERE v.vehicle_num = #{vehicleNum}
			AND r.reservation_status = '완료'
			AND r.reservation_date = CAST(TO_CHAR(NOW(), 'YYYYMMDD') AS INTEGER)
    </select>

</mapper>